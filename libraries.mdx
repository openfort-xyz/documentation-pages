import { ServerLibraries } from '@/components/ServerLibraries'
import { ClientLibraries } from '@/components/ClientLibraries'

export const description =
  'Libraries and tools for interacting with your Openfort integration.'

export const sections = [
  { title: 'Server-side SDKs', id: 'server-side-sdks' },
  { title: 'Client-side SDKs', id: 'client-side-sdks' },
]

# Openfort SDKs

The recommended way to interact with the Openfort API is by using one of our official SDKs.{{ className: 'lead' }}

<ServerLibraries />

<ClientLibraries />

## Openfort OpenAPI Specification

Openfort’s OpenAPI specification empowers you with a broad set of developer tooling, starting with Postman collections.

[Openfort Open API Specification](https://api.openfort.xyz//api-docs/swagger.json)
